services:
  - type: web
    name: pixelbin-app
    runtime: python
    buildCommand: pip install -r requirements.txt
    startCommand: gunicorn app:app
    envVars:
      - key: PIXELBIN_API_TOKEN
        sync: false
